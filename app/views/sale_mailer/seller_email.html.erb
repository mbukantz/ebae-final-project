<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h1>Congrats, <%= @seller.name %>!</h1>
    <h2>You just finalized a sale.</h2>
    <p>
Item: <%= @listing.item.name %><br>
Final Sale Price: <%=number_to_currency(@listing.current_price / 100.0)%><br>
Shipping: <%=number_to_currency(@listing.shipping_price / 100.0)%><br>
----------------------------------<br>
Total: <%=number_to_currency((@listing.shipping_price + @listing.current_price) / 100.0)%><br>
    </p>

<h3>Buyer Information:</h3>
<ul>
  <li>Name: <%= @buyer.name %></li>
  <li>Email: <%= @buyer.email %></li>

  <li>Phone Number: <% @buyer.phones.each do |phone| %>
      <ul>
        <li>Type: <%= phone.label.capitalize %>, Number: <%= phone.number %>
        <% end %>
        </li>
      </ul>
  <li>Address: <% @buyer.locations.each do |location| %>
    <ul>
     <li>
      Type: <%= location.label.capitalize %><br>
       <%= location.street %> <br>
       <%= location.city %>, <%= location.state %> <%= location.zip %>  
       <% end %>
    </li>
  </ul>
  </body>
</html>