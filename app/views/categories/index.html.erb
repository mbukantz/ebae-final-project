
Browse Categories

<%@categories.each do |category|%>
<p>
  <strong><a href="/categories/<%=category.id%>"><%=category.name%></a></strong>
    <%category.items.last(3).each do |item|%>
    <p>
      <a href="/listings/<%=item.listing.id%>"><%=item.name%></a>-<%=number_to_currency(item.listing.current_price / 100.0) if item.listing.current_price %>
    </p>
    <%end%>
</p>
<%end%><br>
