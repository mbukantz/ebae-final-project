<%=render partial:"layouts/header", locals:{title: @listing.item.name}%>



<div class="page-content">
  <div class="container">

    <div>
      <%= render 'watchlist' %>
    </div>

    <div>
      <%= render 'item_description' %>
    </div>

    <div>
      <%= render 'bid_info' %>
    </div>

    <div>
      <%= render 'listing_details' %>
    </div>
  </div>
</div>
<br>
<%= link_to "Ask seller a question about this item", new_listing_message_path(@listing) %>
